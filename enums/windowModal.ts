export enum WindowControlTypes {
  Minimize = 'Minimize',
  Fullscreen = 'Fullscreen',
  Close = 'Close',
}

export enum WindowControlSizes {
  SM = 'SM',
  MD = 'MD',
  LG = 'LG',
}

export enum EventsNames {
  Action = 'action'
}
